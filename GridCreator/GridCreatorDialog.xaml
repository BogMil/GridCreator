<Window x:Class="GridCreator.GridCreatorDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GridCreator"
             xmlns:ViewModel="clr-namespace:GridCreator.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="750" d:DesignWidth="800"
             WindowStyle="ToolWindow"
        >
    <Window.Resources>
        <ViewModel:ChangePageViewModel x:Key="vm"/>
    </Window.Resources>
    <Grid Height="700">
        <StackPanel>
            <Button Height="50" Margin="10 10" Command="{Binding InsertAutoGeneratedCodeCommand, Source={StaticResource vm}}">click me</Button>
        </StackPanel>

        
        <Frame x:Name="testFrame" NavigationUIVisibility="Hidden" Margin="10,85,10,0" Height="550" VerticalAlignment="Top"
            ScrollViewer.VerticalScrollBarVisibility="Visible" 
            ScrollViewer.CanContentScroll="True" 
        />
        
        <Grid Margin="0 650 0 0"  Height="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Grid.Row="1" Margin="10 0 0 0" >
                Back
            </Button>
            <Button Grid.Column="4" Grid.Row="1" Margin="0 0 10 0" Command="{Binding ChangePageCommand, Source={StaticResource vm}}">
                Next
            </Button>
            <Button Grid.Column="2" Grid.Row="1" Margin="0 0 10 0" >
                Finish
            </Button>
        </Grid>
    </Grid>
</Window>
